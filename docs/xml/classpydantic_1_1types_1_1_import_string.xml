<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.15.0" xml:lang="es">
  <compounddef id="classpydantic_1_1types_1_1_import_string" kind="class" language="Python" prot="public">
    <compoundname>pydantic::types::ImportString</compoundname>
    <sectiondef kind="protected-static-func">
      <memberdef kind="function" id="classpydantic_1_1types_1_1_import_string_1a9020e8b0e7761cf4f313b21f5db957f5" prot="protected" static="yes" const="no" explicit="no" inline="no" virt="non-virtual">
        <type>str</type>
        <definition> str pydantic.types.ImportString._serialize</definition>
        <argsstring>(Any v)</argsstring>
        <name>_serialize</name>
        <qualifiedname>pydantic.types.ImportString._serialize</qualifiedname>
        <param>
          <type>Any</type>
          <declname>v</declname>
        </param>
        <briefdescription>
        </briefdescription>
        <detaileddescription>
        </detaileddescription>
        <inbodydescription>
        </inbodydescription>
        <location file="micro_gestion_objetos/venv/Lib/site-packages/pydantic/types.py" line="1017" column="1" bodyfile="micro_gestion_objetos/venv/Lib/site-packages/pydantic/types.py" bodystart="1017" bodyend="1032"/>
      </memberdef>
    </sectiondef>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para><verbatim>A type that can be used to import a Python object from a string.

`ImportString` expects a string and loads the Python object importable at that dotted path.
Attributes of modules may be separated from the module by `:` or `.`, e.g. if `&apos;math:cos&apos;` is provided,
the resulting field value would be the function `cos`. If a `.` is used and both an attribute and submodule
are present at the same path, the module will be preferred.

On model instantiation, pointers will be evaluated and imported. There is
some nuance to this behavior, demonstrated in the examples below.

```python
import math

from pydantic import BaseModel, Field, ImportString, ValidationError

class ImportThings(BaseModel):
    obj: ImportString

# A string value will cause an automatic import
my_cos = ImportThings(obj=&apos;math.cos&apos;)

# You can use the imported function as you would expect
cos_of_0 = my_cos.obj(0)
assert cos_of_0 == 1

# A string whose value cannot be imported will raise an error
try:
    ImportThings(obj=&apos;foo.bar&apos;)
except ValidationError as e:
    print(e)
    &apos;&apos;&apos;
    1 validation error for ImportThings
    obj
      Invalid python path: No module named &apos;foo.bar&apos; [type=import_error, input_value=&apos;foo.bar&apos;, input_type=str]
    &apos;&apos;&apos;

# Actual python objects can be assigned as well
my_cos = ImportThings(obj=math.cos)
my_cos_2 = ImportThings(obj=&apos;math.cos&apos;)
my_cos_3 = ImportThings(obj=&apos;math:cos&apos;)
assert my_cos == my_cos_2 == my_cos_3

# You can set default field value either as Python object:
class ImportThingsDefaultPyObj(BaseModel):
    obj: ImportString = math.cos

# or as a string value (but only if used with `validate_default=True`)
class ImportThingsDefaultString(BaseModel):
    obj: ImportString = Field(default=&apos;math.cos&apos;, validate_default=True)

my_cos_default1 = ImportThingsDefaultPyObj()
my_cos_default2 = ImportThingsDefaultString()
assert my_cos_default1.obj == my_cos_default2.obj == math.cos

# note: this will not work!
class ImportThingsMissingValidateDefault(BaseModel):
    obj: ImportString = &apos;math.cos&apos;

my_cos_default3 = ImportThingsMissingValidateDefault()
assert my_cos_default3.obj == &apos;math.cos&apos;  # just string, not evaluated
```

Serializing an `ImportString` type to json is also possible.

```python
from pydantic import BaseModel, ImportString

class ImportThings(BaseModel):
    obj: ImportString

# Create an instance
m = ImportThings(obj=&apos;math.cos&apos;)
print(m)
#&gt; obj=&lt;built-in function cos&gt;
print(m.model_dump_json())
#&gt; {&quot;obj&quot;:&quot;math.cos&quot;}
```
</verbatim> </para>
    </detaileddescription>
    <location file="micro_gestion_objetos/venv/Lib/site-packages/pydantic/types.py" line="913" column="1" bodyfile="micro_gestion_objetos/venv/Lib/site-packages/pydantic/types.py" bodystart="913" bodyend="1039"/>
    <listofallmembers>
      <member refid="classpydantic_1_1types_1_1_import_string_1a9020e8b0e7761cf4f313b21f5db957f5" prot="protected" virt="non-virtual"><scope>pydantic::types::ImportString</scope><name>_serialize</name></member>
    </listofallmembers>
  </compounddef>
</doxygen>
